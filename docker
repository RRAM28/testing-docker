#!/usr/bin/env bash

# generate ssh key for github
# eval "$(ssh-agent -s)"
# ssh-keygen -t rsa -C "admin@codeflakes.io"
echo "All dependencies for Shotengaai were installed"
echo "please copy the follow shh key on Github for CI Deployment"
echo "==========================================================================================="
cat ~/.ssh/id_rsa.pub
echo "==========================================================================================="
